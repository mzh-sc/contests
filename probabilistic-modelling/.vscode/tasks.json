{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Integrated Terminal: Python",
            "type": "shell",
            "command": "${config:local.condaPath}\\condabin\\conda.bat activate probabilistic-modelling && ${config:python.pythonPath}"
        },
        {
            "label": "Integrated Terminal: Conda",
            "type": "shell",
            "command": "cmd /k ${config:local.condaPath}\\condabin\\conda.bat activate probabilistic-modelling"
        }
    ]
}